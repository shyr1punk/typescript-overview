/**
 * Пример использования библиотеки без декларации её интерфейса
 *
 * IDE не подсказывает интерфейсы
 */
import getLineHeight from 'line-height';


const lineHeight = getLineHeight(document.createElement('textarea'));

